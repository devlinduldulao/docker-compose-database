services:
  redis:
    image: redis:8
    # restart: always
    volumes:
      - redis-data:/data
    ports:
      - "6379:6379"
    networks:
      - dev

volumes:
  redis-data:
    driver: local

networks:
  dev:
    driver: bridge
